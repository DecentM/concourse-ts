id: concourse-ts-resource-terraform
language: typescript
platform: node
type: library

tasks:
  build:
    command: swc
    args:
      - --outputPath
      - $workspaceRoot/dist/packages/concourse-ts-resource-terraform
      - --tsConfig
      - $workspaceRoot/packages/concourse-ts-resource-terraform/tsconfig.lib.json
      - --main
      - $workspaceRoot/packages/concourse-ts-resource-terraform/src/index.ts
      - --assets
      - '["packages/concourse-ts-resource-terraform/*.md"]'
    outputs:
      - '{options.outputPath}'

  lint:
    command: eslint
    args:
      - '["packages/concourse-ts-resource-terraform/**/*.ts"]'
    outputs:
      - '{options.outputFile}'
