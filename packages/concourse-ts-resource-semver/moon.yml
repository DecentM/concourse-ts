id: concourse-ts-resource-semver
language: typescript
platform: node
type: library

tasks:
  build:
    command: swc
    args:
      - --outputPath
      - $workspaceRoot/dist/packages/concourse-ts-resource-semver
      - --tsConfig
      - $workspaceRoot/packages/concourse-ts-resource-semver/tsconfig.lib.json
      - --main
      - $workspaceRoot/packages/concourse-ts-resource-semver/src/index.ts
      - --assets
      - '["packages/concourse-ts-resource-semver/*.md"]'
    outputs:
      - '{options.outputPath}'

  lint:
    command: eslint
    args:
      - '["packages/concourse-ts-resource-semver/**/*.ts"]'
    outputs:
      - '{options.outputFile}'
