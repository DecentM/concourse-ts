{
  "name": "concourse-ts-resource-sonarqube.yaml",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/concourse-ts-resource-sonarqube.yaml/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nrwl/js:tsc",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "updateBuildableProjectDepsInPackageJson": false,
        "outputPath": "dist/packages/concourse-ts-resource-sonarqube.yaml",
        "main": "packages/concourse-ts-resource-sonarqube.yaml/src/index.ts",
        "tsConfig": "packages/concourse-ts-resource-sonarqube.yaml/tsconfig.lib.json",
        "assets": [
          "packages/concourse-ts-resource-sonarqube.yaml/*.md"
        ]
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "packages/concourse-ts-resource-sonarqube.yaml/**/*.ts"
        ]
      }
    }
  },
  "tags": []
}
