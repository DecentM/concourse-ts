id: concourse-ts-resource-sonarqube
language: typescript
platform: node
type: library

tasks:
  build:
    command: swc
    args:
      - --outputPath
      - $workspaceRoot/dist/packages/concourse-ts-resource-sonarqube
      - --tsConfig
      - $workspaceRoot/packages/concourse-ts-resource-sonarqube/tsconfig.lib.json
      - --main
      - $workspaceRoot/packages/concourse-ts-resource-sonarqube/src/index.ts
      - --assets
      - '["packages/concourse-ts-resource-sonarqube/*.md"]'
    outputs:
      - '{options.outputPath}'

  lint:
    command: eslint
    args:
      - '["packages/concourse-ts-resource-sonarqube/**/*.ts"]'
    outputs:
      - '{options.outputFile}'
